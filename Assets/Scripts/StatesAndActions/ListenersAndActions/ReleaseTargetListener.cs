using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Events;

public class ReleaseTargetListener : BaseListener
{
    public ReleaseTargetListener(State state, UnityEvent trig) : base(state, trig)
    {
    }

    public override void ToCall()
    {
        
    }
}
